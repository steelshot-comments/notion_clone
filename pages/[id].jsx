import Head from "next/head";

const page = () => {
  return (
    <>
      {/* <Head>
        <title>{`${title}`}</title>
        <meta name="description" content={`${description}`} />
      </Head> */}
      <h1>Page</h1>
    </>
  );
};

export default page;
